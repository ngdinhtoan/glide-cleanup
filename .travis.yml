language: go

go:
  - 1.5.x
  - 1.6.x
  - 1.7.x
  - 1.8.x
  - tip

# Use container-based infrastructure
sudo: false

install:
  - mkdir -p $GOPATH/bin
  - curl https://glide.sh/get | sh

script:
  - GO15VENDOREXPERIMENT=1 go build
  - glide cleanup --debug
